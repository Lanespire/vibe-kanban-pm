name: Create Release

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      version_type:
        description: "Version bump type"
        required: true
        default: "patch"
        type: choice
        options:
          - patch
          - minor
          - major
          - prerelease

concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: write
  packages: write
  pull-requests: write

env:
  NODE_VERSION: 22
  PNPM_VERSION: 10.13.1
  RUST_TOOLCHAIN: nightly-2025-12-04
  CARGO_XWIN_VERSION: 0.20.2

jobs:
  bump-version:
    runs-on: ubuntu-latest
    outputs:
      new_tag: ${{ steps.version.outputs.new_tag }}
      new_version: ${{ steps.version.outputs.new_version }}
      branch_suffix: ${{ steps.branch.outputs.suffix }}
    steps:
      - name: Cache cargo-edit
        uses: actions/cache@v3
        id: cache-cargo-edit
        with:
          path: ~/.cargo/bin/cargo-set-version
          key: cargo-edit-${{ runner.os }}-${{ env.RUST_TOOLCHAIN }}

      - name: Install cargo-edit
        if: steps.cache-cargo-edit.outputs.cache-hit != 'true'
        run: cargo install cargo-edit

      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ssh-key: ${{ secrets.DEPLOY_KEY }}

      - name: setup node
        uses: ./.github/actions/setup-node

      - name: Generate branch suffix
        id: branch
        run: |
          branch_name="${{ github.ref_name }}"
          # Get last 6 characters of branch name, remove all special chars (including dashes)
          suffix=$(echo "$branch_name" | tail -c 7 | sed 's/[^a-zA-Z0-9]//g' | tr '[:upper:]' '[:lower:]')
          echo "Branch: $branch_name"
          echo "Suffix: $suffix"
          echo "suffix=$suffix" >> $GITHUB_OUTPUT

      - name: Determine and update versions
        id: version
        run: |
          # Get the latest version from npm registry
          latest_npm_version=$(npm view vibe-kanban-pm version 2>/dev/null || echo "0.0.0")
          echo "Latest npm version: $latest_npm_version"

          timestamp=$(date +%Y%m%d%H%M%S)

          # Determine version type (default to patch for push events)
          VERSION_TYPE="${{ github.event.inputs.version_type }}"
          if [[ -z "$VERSION_TYPE" ]]; then
            VERSION_TYPE="patch"
          fi

          # Update root package.json based on npm version, not current package.json
          if [[ "$VERSION_TYPE" == "prerelease" ]]; then
            # For prerelease, use current package.json version and add branch suffix
            npm version prerelease --preid="${{ steps.branch.outputs.suffix }}" --no-git-tag-version

            new_version=$(node -p "require('./package.json').version")
            new_tag="v${new_version}.${timestamp}"
          else
            # For regular releases, use npm version and bump it
            npm version $latest_npm_version --no-git-tag-version --allow-same-version
            npm version $VERSION_TYPE --no-git-tag-version

            new_version=$(node -p "require('./package.json').version")
            new_tag="v${new_version}-${timestamp}"
          fi

          # Update npx-cli package.json to match
          cd npx-cli
          npm version $new_version --no-git-tag-version --allow-same-version
          cd ..

          # Update frontend package.json to match
          cd frontend
          npm version $new_version --no-git-tag-version --allow-same-version
          cd ..

          cargo set-version --workspace "$new_version"

          echo "New version: $new_version"
          echo "new_version=$new_version" >> $GITHUB_OUTPUT
          echo "new_tag=$new_tag" >> $GITHUB_OUTPUT

      - name: Commit changes and create tag
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add package.json package-lock.json pnpm-lock.yaml
          git add npx-cli/package.json npx-cli/package-lock.json
          git add frontend/package.json Cargo.lock
          git add $(find . -name Cargo.toml)

          # Only commit if there are staged changes
          if git diff --staged --quiet; then
            echo "No version changes to commit"
          else
            git commit -m "chore: bump version to ${{ steps.version.outputs.new_version }}"
            git push
          fi

          # Create and push tag regardless
          git tag -a ${{ steps.version.outputs.new_tag }} -m "Release ${{ steps.version.outputs.new_tag }}"
          git push --tags

  build-frontend:
    needs: bump-version
    runs-on: ubuntu-latest
    env:
      VITE_PUBLIC_REACT_VIRTUOSO_LICENSE_KEY: ${{ secrets.PUBLIC_REACT_VIRTUOSO_LICENSE_KEY }}
      VITE_VK_SHARED_API_BASE: ${{ secrets.VK_SHARED_API_BASE }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.bump-version.outputs.new_tag }}

      - name: Setup Node
        uses: ./.github/actions/setup-node

      - name: Install dependencies
        run: pnpm install

      - name: Lint frontend
        run: cd frontend && npm run lint

      - name: Type check frontend
        run: cd frontend && npx tsc --noEmit

      - name: Build frontend
        run: cd frontend && npm run build
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          VITE_POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY }}
          VITE_POSTHOG_API_ENDPOINT: ${{ secrets.POSTHOG_API_ENDPOINT }}

      - name: Create Sentry release
        uses: getsentry/action-release@v3
        continue-on-error: true
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
          SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}
        with:
          release: ${{ needs.bump-version.outputs.new_version }}
          environment: production
          sourcemaps: "./frontend/dist"
          ignore_missing: true

      - name: Upload frontend artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/
          retention-days: 1

  build-backend:
    needs: [bump-version, build-frontend]
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            name: linux-x64
          - target: aarch64-unknown-linux-musl
            os: ubuntu-latest
            name: linux-arm64
          - target: x86_64-pc-windows-msvc
            os: ubuntu-latest
            name: windows-x64
          - target: x86_64-apple-darwin
            os: macos-latest
            name: macos-x64
          - target: aarch64-apple-darwin
            os: macos-latest
            name: macos-arm64
          - target: aarch64-pc-windows-msvc
            os: ubuntu-latest
            name: windows-arm64
    env:
      CARGO_INCREMENTAL: "0"
      SCCACHE_GHA_ENABLED: "true"
      RUSTC_WRAPPER: "sccache"
      SCCACHE_CACHE_SIZE: "10G"
      CARGO_HOME: ${{ github.workspace }}/.cargo
      RUSTUP_HOME: ${{ github.workspace }}/.rustup
      XWIN_CACHE_DIR: ${{ github.workspace }}/.xwin-cache
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.bump-version.outputs.new_tag }}

      - name: Setup sccache
        uses: BloopAI/sccache-action@main

      - name: Cache Rust toolchain
        uses: actions/cache@v4
        with:
          path: .rustup/toolchains
          key: rust-toolchain-${{ runner.os }}-${{ matrix.target }}-${{ env.RUST_TOOLCHAIN }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          targets: ${{ matrix.target }}
          components: rustfmt, clippy

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          DEBIAN_FRONTEND=noninteractive sudo apt-get install -y clang libclang-dev lld llvm nasm cmake ninja-build

          if [[ "${{ matrix.target }}" == *"windows"* ]]; then
            DEBIAN_FRONTEND=noninteractive sudo apt-get install -y clang-19 clang-tools-19 llvm-19 lld-19
            echo "/usr/lib/llvm-19/bin" >> $GITHUB_PATH
          fi

      - name: Cache Cargo registry
        uses: actions/cache@v5
        with:
          path: |
            .cargo/registry/cache
            .cargo/registry/index
            .cargo/git/db
            .cargo/bin
            .cargo/.crates.toml
            .cargo/.crates2.json
          key: cargo-${{ runner.os }}-${{ env.RUST_TOOLCHAIN }}-${{ matrix.target }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            cargo-${{ runner.os }}-${{ env.RUST_TOOLCHAIN }}-${{ matrix.target }}-

      - name: Install Zig
        if: runner.os == 'Linux' && !contains(matrix.target, 'windows')
        uses: BloopAI/setup-zig@main
        with:
          version: 0.15.2

      - name: Install cargo zigbuild
        if: runner.os == 'Linux' && !contains(matrix.target, 'windows')
        run: cargo install --locked cargo-zigbuild --version 0.20.1

      - name: Install cargo xwin
        if: runner.os == 'Linux' && contains(matrix.target, 'windows')
        run: cargo install --locked cargo-xwin --version ${{ env.CARGO_XWIN_VERSION }}

      - name: Cache xwin downloads
        if: runner.os == 'Linux' && contains(matrix.target, 'windows')
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/.xwin-cache
          key: xwin-${{ runner.os }}-${{ matrix.target }}-cargo-xwin-${{ env.CARGO_XWIN_VERSION }}

      - name: Cache target
        uses: actions/cache@v5
        with:
          path: target
          key: target-${{ runner.os }}-${{ env.RUST_TOOLCHAIN }}-${{ matrix.target }}-${{ github.ref_name }}-${{ github.sha }}
          restore-keys: |
            target-${{ runner.os }}-${{ env.RUST_TOOLCHAIN }}-${{ matrix.target }}-${{ github.ref_name }}-
            target-${{ runner.os }}-${{ env.RUST_TOOLCHAIN }}-${{ matrix.target }}-

      - name: Setup cargo-sweep
        shell: bash
        run: |
          cargo install --locked cargo-sweep --version 0.8.0

      - name: Download frontend artifact
        uses: actions/download-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/

      - name: Build backend (Linux)
        if: runner.os == 'Linux' && !contains(matrix.target, 'windows')
        run: |
          cargo zigbuild --release --target ${{ matrix.target }} -p server -p review --bin server --bin mcp_task_server --bin review
        env:
          POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY }}
          POSTHOG_API_ENDPOINT: ${{ secrets.POSTHOG_API_ENDPOINT }}
          VK_SHARED_API_BASE: ${{ secrets.VK_SHARED_API_BASE }}

      - name: Build backend (macOS)
        if: runner.os == 'macOS'
        run: |
          if [[ "${{ matrix.target }}" == "x86_64-apple-darwin" ]]; then
            export MACOSX_DEPLOYMENT_TARGET=10.12
          elif [[ "${{ matrix.target }}" == "aarch64-apple-darwin" ]]; then
            export MACOSX_DEPLOYMENT_TARGET=11.0
          fi
          cargo build --release --target ${{ matrix.target }} -p server -p review --bin server --bin mcp_task_server --bin review
        env:
          POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY }}
          POSTHOG_API_ENDPOINT: ${{ secrets.POSTHOG_API_ENDPOINT }}
          VK_SHARED_API_BASE: ${{ secrets.VK_SHARED_API_BASE }}

      - name: Build backend (Windows)
        if: runner.os == 'Linux' && contains(matrix.target, 'windows')
        run: |
          if [[ "${{ matrix.target }}" == "aarch64-pc-windows-msvc" ]]; then
            chmod +x scripts/ring-cc-wrapper.sh scripts/clang
            export PATH="${{ github.workspace }}/scripts:$PATH"
            export RING_CC=/usr/lib/llvm-19/bin/clang
            export DEFAULT_CC=clang-cl
            export CC_aarch64_pc_windows_msvc="${{ github.workspace }}/scripts/ring-cc-wrapper.sh"
          fi
          cargo xwin build --cross-compiler clang-cl --release --target ${{ matrix.target }} -p server -p review --bin server --bin mcp_task_server --bin review
        env:
          POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY }}
          POSTHOG_API_ENDPOINT: ${{ secrets.POSTHOG_API_ENDPOINT }}
          VK_SHARED_API_BASE: ${{ secrets.VK_SHARED_API_BASE }}
          CARGO_PROFILE_RELEASE_DEBUG: 0

      - name: Setup Sentry CLI
        uses: matbour/setup-sentry-cli@v2
        continue-on-error: true
        with:
          token: ${{ secrets.SENTRY_AUTH_TOKEN }}
          organization: ${{ secrets.SENTRY_ORG }}
          project: ${{ secrets.SENTRY_PROJECT }}
          version: 2.21.2

      - name: Upload source maps to Sentry
        continue-on-error: true
        run: sentry-cli debug-files upload --include-sources target/${{ matrix.target }}/release

      - name: Prepare binaries (non-macOS)
        if: runner.os != 'macOS'
        shell: bash
        run: |
          mkdir -p dist
          if [[ "${{ matrix.name }}" == *"windows"* ]]; then
            cp target/${{ matrix.target }}/release/server.exe dist/vibe-kanban-${{ matrix.name }}.exe
            cp target/${{ matrix.target }}/release/mcp_task_server.exe dist/vibe-kanban-mcp-${{ matrix.name }}.exe
            cp target/${{ matrix.target }}/release/review.exe dist/vibe-kanban-review-${{ matrix.name }}.exe
          else
            cp target/${{ matrix.target }}/release/server dist/vibe-kanban-${{ matrix.name }}
            cp target/${{ matrix.target }}/release/mcp_task_server dist/vibe-kanban-mcp-${{ matrix.name }}
            cp target/${{ matrix.target }}/release/review dist/vibe-kanban-review-${{ matrix.name }}
          fi

      # macOS signing (optional - skipped if secrets not configured)
      - name: Check if Apple signing is configured
        if: runner.os == 'macOS'
        id: check-signing
        run: |
          if [[ -n "${{ secrets.APPLE_CERTIFICATE_P12_BASE64 }}" ]]; then
            echo "signing_enabled=true" >> $GITHUB_OUTPUT
          else
            echo "signing_enabled=false" >> $GITHUB_OUTPUT
            echo "⚠️ Apple signing secrets not configured - binaries will be unsigned"
          fi

      - name: Prepare Apple certificate (macOS)
        if: runner.os == 'macOS' && steps.check-signing.outputs.signing_enabled == 'true'
        run: |
          echo "${{ secrets.APPLE_CERTIFICATE_P12_BASE64 }}" | base64 --decode > certificate.p12

      - name: Write API Key to file
        if: runner.os == 'macOS' && steps.check-signing.outputs.signing_enabled == 'true'
        env:
          API_KEY: ${{ secrets.APP_STORE_API_KEY }}
        run: echo $API_KEY > app_store_key.json

      - name: Sign main binary (macOS)
        if: runner.os == 'macOS' && steps.check-signing.outputs.signing_enabled == 'true'
        uses: BloopAI/apple-code-sign-action@v1
        with:
          input_path: target/${{ matrix.target }}/release/server
          output_path: vibe-kanban
          p12_file: certificate.p12
          p12_password: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          sign: true
          sign_args: "--code-signature-flags=runtime"

      - name: Package signed main binary (macOS)
        if: runner.os == 'macOS' && steps.check-signing.outputs.signing_enabled == 'true'
        run: zip vibe-kanban.zip vibe-kanban

      - name: Sign MCP binary (macOS)
        if: runner.os == 'macOS' && steps.check-signing.outputs.signing_enabled == 'true'
        uses: BloopAI/apple-code-sign-action@v1
        with:
          input_path: target/${{ matrix.target }}/release/mcp_task_server
          output_path: vibe-kanban-mcp
          p12_file: certificate.p12
          p12_password: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          sign: true
          sign_args: "--code-signature-flags=runtime"

      - name: Package signed MCP binary (macOS)
        if: runner.os == 'macOS' && steps.check-signing.outputs.signing_enabled == 'true'
        run: zip vibe-kanban-mcp.zip vibe-kanban-mcp

      - name: Sign Review binary (macOS)
        if: runner.os == 'macOS' && steps.check-signing.outputs.signing_enabled == 'true'
        uses: BloopAI/apple-code-sign-action@v1
        with:
          input_path: target/${{ matrix.target }}/release/review
          output_path: vibe-kanban-review
          p12_file: certificate.p12
          p12_password: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          sign: true
          sign_args: "--code-signature-flags=runtime"

      - name: Package signed Review binary (macOS)
        if: runner.os == 'macOS' && steps.check-signing.outputs.signing_enabled == 'true'
        run: zip vibe-kanban-review.zip vibe-kanban-review

      - name: Notarize signed binaries (macOS)
        if: runner.os == 'macOS' && steps.check-signing.outputs.signing_enabled == 'true'
        uses: BloopAI/apple-code-sign-action@main
        continue-on-error: true
        with:
          input_path: |
            vibe-kanban.zip
            vibe-kanban-mcp.zip
            vibe-kanban-review.zip
          sign: false
          notarize: true
          app_store_connect_api_key_json_file: app_store_key.json

      - name: Prepare signed binaries (macOS)
        if: runner.os == 'macOS' && steps.check-signing.outputs.signing_enabled == 'true'
        run: |
          mkdir -p dist
          cp vibe-kanban.zip dist/vibe-kanban-${{ matrix.name }}.zip
          cp vibe-kanban-mcp.zip dist/vibe-kanban-mcp-${{ matrix.name }}.zip
          cp vibe-kanban-review.zip dist/vibe-kanban-review-${{ matrix.name }}.zip

      - name: Clean up certificates (macOS)
        if: runner.os == 'macOS' && steps.check-signing.outputs.signing_enabled == 'true'
        run: |
          rm -f certificate.p12
          rm -rf private_keys/

      # macOS unsigned binaries (when signing not configured)
      - name: Package unsigned binaries (macOS)
        if: runner.os == 'macOS' && steps.check-signing.outputs.signing_enabled != 'true'
        run: |
          mkdir -p dist
          # Copy and rename binaries to expected names before zipping
          cp target/${{ matrix.target }}/release/server vibe-kanban
          cp target/${{ matrix.target }}/release/mcp_task_server vibe-kanban-mcp
          cp target/${{ matrix.target }}/release/review vibe-kanban-review
          zip -j dist/vibe-kanban-${{ matrix.name }}.zip vibe-kanban
          zip -j dist/vibe-kanban-mcp-${{ matrix.name }}.zip vibe-kanban-mcp
          zip -j dist/vibe-kanban-review-${{ matrix.name }}.zip vibe-kanban-review

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-binary-${{ matrix.name }}
          path: dist/
          retention-days: 1

      - name: Sweep Cargo target cache
        shell: bash
        run: |
          cargo sweep --maxsize 10GB
          cargo sweep --time 30

  package-binaries-for-release:
    needs: [bump-version, build-frontend, build-backend]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: linux-x64
            binary: vibe-kanban
            mcp_binary: vibe-kanban-mcp
            review_binary: vibe-kanban-review
          - name: windows-x64
            binary: vibe-kanban.exe
            mcp_binary: vibe-kanban-mcp.exe
            review_binary: vibe-kanban-review.exe
          - name: macos-x64
            binary: vibe-kanban
            mcp_binary: vibe-kanban-mcp
            review_binary: vibe-kanban-review
          - name: macos-arm64
            binary: vibe-kanban
            mcp_binary: vibe-kanban-mcp
            review_binary: vibe-kanban-review
          - name: windows-arm64
            binary: vibe-kanban.exe
            mcp_binary: vibe-kanban-mcp.exe
            review_binary: vibe-kanban-review.exe
          - name: linux-arm64
            binary: vibe-kanban
            mcp_binary: vibe-kanban-mcp
            review_binary: vibe-kanban-review
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.bump-version.outputs.new_tag }}

      - name: Download backend binary artifact
        uses: actions/download-artifact@v4
        with:
          name: backend-binary-${{ matrix.name }}
          path: dist/

      - name: List downloaded artifacts
        run: |
          echo "Downloaded backend binaries:"
          find dist/

      - name: Create platform package (non-macOS)
        if: matrix.name != 'macos-arm64' && matrix.name != 'macos-x64'
        run: |
          mkdir -p release-binaries
          mkdir vibe-kanban-${{ matrix.name }}
          mkdir vibe-kanban-mcp-${{ matrix.name }}
          mkdir vibe-kanban-review-${{ matrix.name }}

          cp dist/vibe-kanban-${{ matrix.name }}* vibe-kanban-${{ matrix.name }}/${{ matrix.binary }}
          cp dist/vibe-kanban-mcp-${{ matrix.name }}* vibe-kanban-mcp-${{ matrix.name }}/${{ matrix.mcp_binary }}
          cp dist/vibe-kanban-review-${{ matrix.name }}* vibe-kanban-review-${{ matrix.name }}/${{ matrix.review_binary }}

          zip -j release-binaries/vibe-kanban-${{ matrix.name }}.zip vibe-kanban-${{ matrix.name }}/${{ matrix.binary }}
          zip -j release-binaries/vibe-kanban-mcp-${{ matrix.name }}.zip vibe-kanban-mcp-${{ matrix.name }}/${{ matrix.mcp_binary }}
          zip -j release-binaries/vibe-kanban-review-${{ matrix.name }}.zip vibe-kanban-review-${{ matrix.name }}/${{ matrix.review_binary }}

      - name: Create platform package (macOS)
        if: matrix.name == 'macos-arm64' || matrix.name == 'macos-x64'
        run: |
          mkdir -p release-binaries
          # macOS binaries are already zipped from build step
          cp dist/vibe-kanban-${{ matrix.name }}* release-binaries/vibe-kanban-${{ matrix.name }}.zip
          cp dist/vibe-kanban-mcp-${{ matrix.name }}* release-binaries/vibe-kanban-mcp-${{ matrix.name }}.zip
          cp dist/vibe-kanban-review-${{ matrix.name }}* release-binaries/vibe-kanban-review-${{ matrix.name }}.zip

      - name: Upload release binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-binary-${{ matrix.name }}
          path: release-binaries/
          retention-days: 1

  create-release:
    needs: [bump-version, build-frontend, package-binaries-for-release]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.bump-version.outputs.new_tag }}

      - name: Download frontend artifact
        uses: actions/download-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/

      - name: Download all release binaries
        uses: actions/download-artifact@v4
        with:
          pattern: release-binary-*
          path: release-binaries/
          merge-multiple: true

      - name: List downloaded artifacts
        run: |
          echo "Frontend dist:"
          find frontend/dist
          echo "Release binaries:"
          find release-binaries/

      - name: Zip frontend
        run: |
          mkdir vibe-kanban-${{ needs.bump-version.outputs.new_tag }}
          mv frontend/dist vibe-kanban-${{ needs.bump-version.outputs.new_tag }}
          zip -r vibe-kanban-${{ needs.bump-version.outputs.new_tag }}.zip vibe-kanban-${{ needs.bump-version.outputs.new_tag }}

      - name: Setup Node for npm pack
        uses: ./.github/actions/setup-node

      - name: Inject GitHub repo and tag, then Pack
        run: |
          cd npx-cli
          # Replace placeholders with actual values
          sed -i "s|__GITHUB_REPO__|${{ github.repository }}|g" bin/download.js
          sed -i "s|__BINARY_TAG__|${{ needs.bump-version.outputs.new_tag }}|g" bin/download.js
          npm pack

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.bump-version.outputs.new_tag }}
          name: Release ${{ needs.bump-version.outputs.new_tag }}
          prerelease: false
          generate_release_notes: true
          files: |
            vibe-kanban-${{ needs.bump-version.outputs.new_tag }}.zip
            npx-cli/vibe-kanban-*.tgz
            release-binaries/vibe-kanban-linux-x64.zip
            release-binaries/vibe-kanban-linux-arm64.zip
            release-binaries/vibe-kanban-windows-x64.zip
            release-binaries/vibe-kanban-windows-arm64.zip
            release-binaries/vibe-kanban-macos-x64.zip
            release-binaries/vibe-kanban-macos-arm64.zip
            release-binaries/vibe-kanban-mcp-linux-x64.zip
            release-binaries/vibe-kanban-mcp-linux-arm64.zip
            release-binaries/vibe-kanban-mcp-windows-x64.zip
            release-binaries/vibe-kanban-mcp-windows-arm64.zip
            release-binaries/vibe-kanban-mcp-macos-x64.zip
            release-binaries/vibe-kanban-mcp-macos-arm64.zip
            release-binaries/vibe-kanban-review-linux-x64.zip
            release-binaries/vibe-kanban-review-linux-arm64.zip
            release-binaries/vibe-kanban-review-windows-x64.zip
            release-binaries/vibe-kanban-review-windows-arm64.zip
            release-binaries/vibe-kanban-review-macos-x64.zip
            release-binaries/vibe-kanban-review-macos-arm64.zip

      - name: Configure npm authentication
        run: |
          echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc

      - name: Publish to npm
        run: |
          cd npx-cli
          PACKAGE_FILE=$(ls *.tgz | head -n1)
          echo "Publishing $PACKAGE_FILE to npm..."
          npm publish "$PACKAGE_FILE"
          echo "✅ Successfully published to npm!"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
